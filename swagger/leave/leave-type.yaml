tags:
- name: Leave Type
  description: Leave Type API

paths:
# [GET] Leave Type
  /api/leave-Type:
    get:
      tags:
      - Leave Type
      summary: Find All Leave Type
      responses:
        200:
          description: 'OK'
        500:
          description: 'Server Error'

# [POST] Leave Type
    post:
      tags:
      - Leave Type
      summary: Add leave Leave Type
      parameters:
      - name: body
        in: body
        description: leave Leave Type Data
        required: true
        schema:
          $ref: '#/definitions/Leave Type'
      responses:
        201:
          description: 'Created'
        400:
          description: 'Bad Request'
        409:
          description: 'Conflict'
        500:
          description: 'Server Error'

# [GET] Leave Type/id
  /api/leave-type/{id}:
    get:
      tags:
      - Leave Type
      summary: Find leave Leave Type By Id
      parameters:
      - name: id
        in: path
        description: leave Leave Type Id
        required: true
      responses:
        200:
          description: 'OK'
        409:
          description: 'Conflict'
        500:
          description: 'Server Error'

# [PUT] Leave Type/id
    put:
      tags:
      - Leave Type
      summary: Update leave Leave Type By Id
      parameters:
      - name: id
        in: path
        description: leave Leave Type Id
        required: true
      - name: body
        in: body
        description: leave Leave Type Data
        required: true
        schema:
          $ref: '#/definitions/Leave Type'
      responses:
        200:
          description: 'OK'
        400:
          description: 'Bad Request'
        409:
          description: 'Conflict'
        500:
          description: 'Server Error'

# [DELETE] leave-type/id
    delete:
      tags:
      - Leave Type
      summary: Delete leave Leave Type By Id
      parameters:
      - name: id
        in: path
        description: leave Leave Type Id
        required: true
        schema:
          type: string
      security:
        - jwt: []
      responses:
        200:
          description: 'OK'
        409:
          description: 'Conflict'
        500:
          description: 'Server Error'

# definitions
definitions:
  Leave Type:
    type: object
    required:
        - from_date
        - start_time
        - to_date
    properties:
      id:
        type: string
        description: Leave Type Id
      leave_name:
        type: string
        description: leave name
       max_leaves :
        type: string
        description: Max leave
      applicable_after:
        type: string
        description: Applicable after
      max_continous_days:
        type: string
        description: Maximum number day to continue
      is_carry_forward:
        type: boolean
        description: carried over from prev leave
      is_without_pay:
        type: boolean
        description: Leave without pay
      is_optional:
        type: boolean
        description: Leave is optional
      allow_negative_balance:
        type: boolean
      include_holiday_leaves:
        type: boolean
      is_compensatory:
        type: boolean

schemes:
 - https
 - http
